# TES Controller Documentation

This directory contains the Sphinx authoring environment for the TES Controller
project.

## 📚 Contents

### reStructuredText pages
- `index.rst` – Landing page and master ToC
- `installation.rst` – Platform-specific setup
- `getting-started.rst` – Day-one workflow guide
- `usage.rst` – Recipes and quick snippets
- `examples.rst` – End-to-end scripts
- `api.rst` – Autogenerated API reference

### Helper docs
- `README.md` – You are here
- `DOCUMENTATION_SETUP.md` – Deployment walkthrough
- `QUICK_REFERENCE.md` – One-page API cheat sheet

### Configuration & tooling
- `conf.py` – Sphinx configuration (autodoc, napoleon, type hints)
- `requirements.txt` – Doc build dependencies
- `Makefile` / `make.bat` – Build helpers (Unix/Windows)
- `_static/` – Custom static assets
- `.nojekyll` – Ensures nested directories are served by GitHub Pages

## 🚀 Build Locally

```bash
pip install -r docs/requirements.txt
make -C docs html
```

Open `docs/_build/html/index.html` in your browser when the build finishes.

### Clean & rebuild

```bash
make -C docs clean html
```

### Live reload during authoring

```bash
pip install sphinx-autobuild
sphinx-autobuild docs docs/_build/html
```

## 🌐 Publish Options

### GitHub Pages
1. Enable Pages for the repository and choose the `docs/` folder as the source.
2. Copy `docs/_build/html` into `docs/` (or automate via GitHub Actions).
3. Commit and push – GitHub Pages redeploys automatically.

### Read the Docs (optional)
1. Log in at <https://readthedocs.org/> with your GitHub account.
2. Import the repository; RTD auto-detects `docs/conf.py`.
3. Trigger a build to publish at `https://<project>.readthedocs.io/`.

## 🛠️ Updating Content
- Add new pages → create `docs/new-page.rst`, reference it in `index.rst`, rebuild.
- Refresh API docs → update Python docstrings and rerun `make -C docs html`.
- Ship new examples → edit `docs/examples.rst` and ensure scripts execute cleanly.

## ❗ Troubleshooting

| Issue | Resolution |
|-------|------------|
| `ModuleNotFoundError: tes_controller` | Run builds from repo root; confirm `conf.py` manipulates `sys.path`. |
| Missing CSS/JS on GitHub Pages | Ensure `.nojekyll` is present and assets were copied. |
| Sphinx warnings | Run `make -C docs clean html` and address reported issues. |

## 📎 Resources
- [Sphinx documentation](https://www.sphinx-doc.org/)
- [Read the Docs guide](https://docs.readthedocs.io/)
- [reStructuredText primer](https://www.sphinx-doc.org/en/master/usage/restructuredtext/basics.html)
